# v0.1.0 release hardening

Target candidate: `v0.1.0-rc.1`. This plan records evidence; unchecked items are
release blockers unless explicitly accepted with rationale. No post-MVP resource
work belongs in this plan.

## Baseline

- Branch `mvp` contains the five-resource MVP and batched `0.1.0` notes.
- Provider uses Terraform Plugin Framework protocol 6.
- PR CI runs lint, unit tests, build, Terraform formatting, and Changie checks.
- Main CI builds only; public publishing is disabled.
- Team create/update/import/destroy passed against the hosted API on 2026-09-09.
- Other resources reached live create/read/update, but the topology teardown was
  blocked by the retained integration reference documented in known issues.

## Resource readiness matrix

| Resource | CRUD | Import/clean plan | Drift | External deletion | 404 | 409/422 | Replacement | Secrets | Live ACC |
|---|---|---|---|---|---|---|---|---|---|
| team | Passed once | Import passed; clean plan not separately recorded | Pending | Pending | Unit behavior only | Pending | Organization pending | N/A | Partial |
| schedule | Create/read/update reached API; destroy unproven | Import blocked by topology teardown | Pending | Pending | Unit behavior only | Pending | Team/organization pending | N/A | Partial |
| escalation policy | Create/read/update reached API; destroy blocked | Import blocked by topology teardown | Pending | Pending | Unit behavior only | Live 409 observed; 422 pending | Team/organization pending | N/A | Blocked |
| integration | Create/read/update/delete reached API | Import ignores create-only key; clean plan unproven | Pending | Pending | Unit behavior only | Pending | Type/team/organization pending | Unit preservation passed | Partial |
| integration policy | Create/read/update reached API; destroy unproven | Import blocked by topology teardown | Pending | Pending | Unit behavior only | Pending | Scope/organization pending | N/A | Partial |

## Global gates

- [x] Go unit/client tests pass locally.
- [x] Terraform example formatting passes.
- [x] Changie validation passes and `0.1.0` notes are batched.
- [x] Full static/lint suite passes locally on 2026-09-15.
- [ ] All five resources pass live lifecycle validation.
- [ ] Full topology apply and immediate plan are clean.
- [ ] Refresh and post-refresh plan are clean.
- [ ] Full topology destroy reaches zero state and zero remote objects.
- [ ] The entire topology lifecycle succeeds twice from clean environments.
- [ ] Imports produce clean plans for all resources.
- [ ] Drift is detected and reconciled for every mutable resource.
- [ ] External deletion produces recreation plans for every resource.
- [ ] Immutable changes show and execute correct replacement behavior.
- [x] One-time integration secret preservation has unit coverage.
- [ ] One-time integration secret preservation has live clean-plan evidence.
- [x] GoReleaser v2.18.1 built and inspected 14 platform archives; archive
      names, embedded binary name/version, protocol-6 manifest, and all
      checksums passed locally on 2026-09-15.
- [ ] A dedicated RSA release key exists and checksum signing is verified.
- [x] GoReleaser checksum signing was exercised with a disposable RSA key and
      the binary detached signature passed `gpg --verify` on 2026-09-15.
- [x] Publishing workflow is prepared and hard-disabled.
- [ ] Retained integration-reference defect is fixed or explicitly accepted as
      an RC blocker with a tested teardown path.
- [ ] Repository licensing decision is made and a license file is present.

## Release infrastructure decisions

- GoReleaser follows HashiCorp's provider scaffolding conventions.
- The Registry manifest declares protocol `6.0`.
- Release archives inject the semantic version into `main.version`.
- Checksums cover archives and the renamed Registry manifest.
- GPG signs the checksum file with a binary detached signature.
- The release workflow is manual and snapshot-only; its publishing job is
  disabled and configured to create drafts when eventually enabled.
- The RC tag is not represented as a separate Changie version, preserving the
  existing `0.1.0` notes for the final release.

## Required live evidence

Use a new short-lived token, matching CSRF token, and disposable organization.
Record sanitized command results and identifiers here for two topology runs,
then complete per-resource import, drift, external-deletion, error, replacement,
and secret checks. Do not paste credentials, state, API keys, or secret-bearing
payloads.

## Current blockers

1. No current live credentials or disposable organization were supplied for
   this release-hardening run; previously supplied JWTs are expired.
2. The backend retained-reference defect can prevent deterministic topology
   destroy and therefore blocks an evidence-based RC claim until retested/fixed.
3. GnuPG is installed and the GoReleaser signing path is verified, but no
   dedicated production release key or GitHub signing secrets are configured.
4. The repository has no license file; the owner must select a license before a
   public release.
