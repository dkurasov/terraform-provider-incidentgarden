provider_installation {
  filesystem_mirror {
    path    = "MIRROR_PATH"
    include = ["registry.terraform.io/dkurasov/incidentgarden"]
  }

  direct {
    exclude = ["registry.terraform.io/dkurasov/incidentgarden"]
  }
}
